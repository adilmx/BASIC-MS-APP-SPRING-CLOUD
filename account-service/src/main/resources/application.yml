spring:
  application:
    name: ACCOUNT-SERVICE
  datasource:
    url: jdbc:h2:mem:accountMsDB
  cloud:
    discovery:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8889/eureka
server:
  port: 8081
